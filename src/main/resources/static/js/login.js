/*let submitBtn = document.getElementById('submit');
let moveIcon = document.getElementById('move');
let i = 0;
submitBtn.addEventListener('click', () => {
    moveIcon.classList.add('moveLogin');
    console.log('hello');
    setTimeout(() => {
      moveIcon.innerHTML = '<span class="material-symbols-outlined">progress_activity</span>';
      moveIcon.classList.add('rotate');
    },500);
});*/